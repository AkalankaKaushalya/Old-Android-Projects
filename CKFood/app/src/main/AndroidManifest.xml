<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ckfood.pvt">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CKFood">
        <activity android:name=".My_Profile"
            android:theme="@style/profile"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Register_Page"
            android:theme="@style/rgister" />
        <activity
            android:name=".Loging_Page"
            android:theme="@style/logactiviti" />
        <activity
            android:name=".Splash_Screen"
            android:theme="@style/Splash_Theme"></activity>
        <activity
            android:name=".Welcome_Screen"
            android:theme="@style/transperent" />
    </application>

</manifest>